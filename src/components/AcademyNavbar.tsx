// AcademyNavbar — wraps existing Navbar with GA branding overrides
// The Navbar.tsx component is the full implementation; this re-exports it
// with updated navItems injected via the same interface
export { default } from './Navbar';
